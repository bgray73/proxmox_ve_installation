[defaults]
inventory = inventory.json
remote_user = root
private_key_file = ~/.ssh/id_ed25519
host_key_checking = False
retry_files_enabled = False
forks = 10

# Uncomment for verbose diffs on config changes:
# diff = True
